*Virtual machines + Docker must be stopped*

Under settings, we have network settings

Change IPv4 Address assignment to static

Set the following as desired
- ipv4 address
- ipv4 gateway
- dns server

